# 🏗️ Архитектура приложения

## Диаграмма потока данных

```
┌─────────────────────────────────────────────────────────┐
│                    ПОЛЬЗОВАТЕЛЬ                         │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  index.html                             │
│  ┌──────────┐ ┌──────────┐ ┌────────────────────┐      │
│  │ Telegram │ │ manifest │ │ style.css          │      │
│  │  WebApp  │ │  .json   │ │ theme + animations │      │
│  └──────────┘ └──────────┘ └────────────────────┘      │
└─────────────────────────────────────────────────────────┘
   │          │              │           │
   │          │              │           └─→ sw.js (Service Worker)
   │          │              │
   └──────────┼──────┬───────┘
              │      │
              ▼      ▼
        ┌─────────────────────────────────────┐
        │      ES6 Modules (modules/)         │
        │  ┌─────────────────────────────────┐│
        │  │  utils.module.js                ││
        │  │  • UUID, formatting             ││
        │  │  • Telegram API                 ││
        │  │  • Helper functions             ││
        │  └─────────────────────────────────┘│
        │  ┌─────────────────────────────────┐│
        │  │  storage.module.js              ││
        │  │  • localStorage                 ││
        │  │  • IndexedDB                    ││
        │  │  • Backup/restore               ││
        │  └─────────────────────────────────┘│
        │  ┌─────────────────────────────────┐│
        │  │  api.module.js                  ││
        │  │  • HTTP requests                ││
        │  │  • Error handling               ││
        │  │  • Timeouts                     ││
        │  └─────────────────────────────────┘│
        │  ┌─────────────────────────────────┐│
        │  │  chat.module.js                 ││
        │  │  • Manage chats                 ││
        │  │  • Messages                     ││
        │  │  • Search                       ││
        │  └─────────────────────────────────┘│
        │  ┌─────────────────────────────────┐│
        │  │  tasks.module.js                ││
        │  │  • Task groups                  ││
        │  │  • Statistics                   ││
        │  │  • Points calculation           ││
        │  └─────────────────────────────────┘│
        │  ┌─────────────────────────────────┐│
        │  │  error.module.js                ││
        │  │  • Error modals                 ││
        │  │  • Toast notifications          ││
        │  │  • Global error handler         ││
        │  └─────────────────────────────────┘│
        │  ┌─────────────────────────────────┐│
        │  │  ux.module.js                   ││
        │  │  • Draft saving                 ││
        │  │  • Badges                       ││
        │  │  • Shortcuts                    ││
        │  │  • Swipe gestures               ││
        │  └─────────────────────────────────┘│
        └─────────────────────────────────────┘
              │    │     │      │     │     │
              └────┴─────┴──────┴─────┴─────┘
                          │
                          ▼
        ┌──────────────────────────────────┐
        │   STORAGE LAYER                  │
        │  ┌──────────────────────────────┐│
        │  │ localStorage (synchronous)   ││
        │  └──────────────────────────────┘│
        │  ┌──────────────────────────────┐│
        │  │ IndexedDB (large data)       ││
        │  └──────────────────────────────┘│
        │  ┌──────────────────────────────┐│
        │  │ memStore (fallback)          ││
        │  └──────────────────────────────┘│
        └──────────────────────────────────┘
                          │
                          ▼
        ┌──────────────────────────────────┐
        │   SERVICE WORKER (sw.js)         │
        │  ┌──────────────────────────────┐│
        │  │ Network-first (API)          ││
        │  │ Cache-first (static)         ││
        │  │ Offline support              ││
        │  │ Background sync              ││
        │  └──────────────────────────────┘│
        └──────────────────────────────────┘
                          │
                          ▼
        ┌──────────────────────────────────┐
        │   API SERVER                     │
        │   https://lsd-server.onrender... │
        └──────────────────────────────────┘
```

## Поток обработки запроса

```
USER INPUT
    │
    └──→ EVENT LISTENER (click, input, submit)
         │
         └──→ Module Function
              │
              ├──→ [API.module.js] → postJSON()
              │    │
              │    └──→ Network Priority:
              │         ├─ Try: fetch() to server
              │         ├─ If OK: cache response + return
              │         └─ If FAIL: return from cache
              │
              ├──→ [Storage.module.js] → sSet/sGet
              │    │
              │    └──→ Store Priority:
              │         ├─ Try: localStorage
              │         ├─ Fallback: IndexedDB
              │         └─ Final: memStore
              │
              ├──→ [Chat/Tasks.module.js] → business logic
              │
              ├──→ [Error.module.js] → handle errors
              │    │
              │    └──→ Show Modal + Log to Server
              │
              └──→ [UX.module.js] → enhance experience
                   │
                   └──→ Auto-save, badges, toast...
                        │
                        └──→ Update DOM
                             │
                             └──→ Dispatch event for other modules
                                  │
                                  └──→ UI UPDATE (visible to user)
```

## Компоненты системы

### 1. Presentation Layer (UI)
```
┌─────────────────────────────────────┐
│        HTML Elements                │
├─────────────────────────────────────┤
│ Chats     Tasks     Chat Input      │
│ Profile   Modal     Notifications   │
└─────────────────────────────────────┘
         │
         │ Event listeners
         │
  Event Delegation Pattern:
  Single listener on parent
  ↓ Handles all children
```

### 2. Business Logic Layer
```
┌─────────────────────────────────────┐
│    Core Modules (modules/*)         │
├─────────────────────────────────────┤
│ chat.module.js    (Chat operations) │
│ tasks.module.js   (Task operations) │
│ api.module.js     (API calls)       │
│ error.module.js   (Error handling)  │
└─────────────────────────────────────┘
```

### 3. Data Layer
```
┌─────────────────────────────────────┐
│      Storage Abstraction            │
├─────────────────────────────────────┤
│ localStorage (fast, small)          │
│ IndexedDB    (large data)           │
│ memStore     (fallback)             │
│ Server       (source of truth)      │
└─────────────────────────────────────┘
```

### 4. Network Layer
```
┌─────────────────────────────────────┐
│     Service Worker + Fetch          │
├─────────────────────────────────────┤
│ Network-first for APIs              │
│ Cache-first for static              │
│ Offline support                     │
│ Background sync                     │
└─────────────────────────────────────┘
```

## Иерархия модулей

```
                    index.html
                        │
        ┌───────────────┼───────────────┐
        │               │               │
    style.css       manifest.json    sw.js
        │
    ┌───┴────────────────────────────┐
    │                                │
utils.module.js (foundation)        
    │
    ├──→ storage.module.js
    │    │
    │    └──→ api.module.js
    │         │
    │         └──→ chat.module.js
    │         │
    │         └──→ tasks.module.js
    │
    ├──→ error.module.js
    │    │
    │    └──→ (all modules call this)
    │
    └──→ ux.module.js
         │
         └──→ (uses all other modules)
```

## Lifecycle приложения

```
1. USER OPENS APP
   └──→ Browser loads index.html

2. META TAGS & MANIFEST
   └──→ PWA meta tags loaded
   └──→ manifest.json parsed
   └──→ Browser checks if installable

3. SERVICE WORKER REGISTRATION
   └──→ sw.js registers
   └──→ Static assets cached
   └──→ Ready for offline

4. MODULES INITIALIZATION
   └──→ utils.module (lowest)
   └──→ storage.module (with utils)
   └──→ api.module (with utils, storage)
   └──→ chat.module (with all above)
   └──→ tasks.module (with all above)
   └──→ error.module (with utils)
   └──→ ux.module (with chat, storage, utils)

5. OLD CODE (JavaScript.js)
   └──→ Still loads for compatibility
   └──→ Gradually migrate to modules

6. EVENT LISTENERS ATTACHED
   └──→ UI ready for user interaction

7. BACKGROUND TASKS
   └──→ SW syncs offline changes
   └──→ Notifications check
   └──→ Auto-save drafts

8. USER INTERACTION
   └──→ Chats, tasks, API calls
   └──→ Offline queueing (if offline)
   └──→ Error handling
   └──→ Local storage

9. APP CLOSE
   └──→ Data persisted in storage
   └──→ SW stays active in background
   └──→ Ready for next session
```

## Dependency Graph

```
Lowest level (no dependencies):
  utils.module.js

Mid level (depends on utils):
  storage.module.js
  error.module.js

Higher level (depends on above):
  api.module.js

Business logic (depends on everything):
  chat.module.js
  tasks.module.js

Top level (depends on all):
  ux.module.js

Service Worker (independent):
  sw.js

Presentation (uses all):
  index.html
```

## Performance Characteristics

```
Module Loading:
┌──────────────────────────────────┐
│ utils (small)          instant   │
│ storage (medium)       instant   │
│ api (small)            instant   │
│ chat (large)           fast      │
│ tasks (large)          fast      │
│ error (small)          instant   │
│ ux (large)             fast      │
└──────────────────────────────────┘

Total: ~50ms all modules loaded
Browser cache: ~10ms next time

Storage Operations:
├─ localStorage: <1ms
├─ IndexedDB: 1-5ms
└─ memStore: <0.1ms

Network Operations:
├─ Network available: 100-2000ms
├─ Timeout: 20000ms
└─ Offline: 0ms (uses cache)
```

## Expansion Points (как добавить функции)

```
1. Новый модуль функционала?
   └──→ Создайте modules/feature.module.js
   └──→ Импортируйте нужные зависимости
   └──→ Экспортируйте публичные функции
   
2. Новая API endpoint?
   └──→ Добавьте в api.module.js
   └──→ Обработайте ошибки
   └──→ Обновите логирование

3. Новое хранилище данных?
   └──→ Добавьте в storage.module.js
   └──→ Используйте IndexedDB для больших объемов
   └──→ Добавьте в STORAGE constants

4. Новая UI фишка?
   └──→ Добавьте стили в style.css
   └──→ Добавьте логику в ux.module.js
   └──→ Подключите в index.html если модаль
```

Архитектура готова для масштабирования! 🚀
